/**
 * dsh-bluewhale-pet — host half.
 * The browser half registers the pixel themes and mounts the companion pet.
 * The host half currently contributes no server-side logic; the mini-agent
 * bridge (subagent-backed task execution) lands here in a later step.
 */

/** @type {import('@deepseek-ai/cordis').Plugin} */
export function apply() {}
